<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Dadu"
        tools:targetApi="31">
        <activity
            android:name=".pendingorder"
            android:exported="false" />
        <activity
            android:name=".ui.OrderDetailsActivity"
            android:exported="false" />
        <activity
            android:name=".ui.PendingOrdersActivity"
            android:exported="false" /> <!-- Remove or correct this meta-data reference -->
        <!-- If you don't need it, remove it -->
        <!--
<meta-data
            android:name="com.google.android.actions"
            android:resource="@xml/strings" />
        -->
        <activity
            android:name=".LanguageActivity"
            android:exported="false" />
        <activity
            android:name=".SplashActivity"
            android:exported="false" />
        <activity
            android:name=".ForgotMyPassword"
            android:exported="false" />
        <activity
            android:name=".Maininterface"
            android:exported="false" />
        <activity
            android:name=".SignupActivity"
            android:exported="false" />
        <activity
            android:name=".MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>